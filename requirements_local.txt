# Local model support dependencies
# Install with: pip install -r requirements_local.txt
# Use this for local model testing (GPT-2, LLaMA, Mistral, etc.)

torch>=2.0.0
transformers>=4.40.0
accelerate>=0.20.0
tokenizers>=0.15.0
huggingface-hub>=0.22.0
safetensors>=0.4.2
sentencepiece>=0.1.99
psutil>=5.9.0

# Optional: for specific model architectures
# bitsandbytes>=0.41.0  # For quantization
# flash-attn>=2.0.0     # For flash attention
